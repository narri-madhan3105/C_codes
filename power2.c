#include<stdio.h>
#include<math.h>
// %f or .1f% for float answer (with decimal values)
// 0.f% for no decimal value
int main(){
    printf("the power of 4 to5 is %.3f", pow(4,5));
    return 0;
}